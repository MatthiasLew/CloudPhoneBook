[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cloudphonebook-shared"
version = "0.1.0"
description = "Shared DTOs and utilities for CloudPhoneBook"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "email-validator>=2.0"
]
